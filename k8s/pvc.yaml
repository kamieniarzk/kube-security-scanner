apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kube-security-scanner-pvc
  namespace: kube-security-scanner
spec:
  accessModes:
   - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
